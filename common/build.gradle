// Parent build file for all common subprojects
// Each subproject automatically applies the shared configuration

subprojects {
    apply from: rootProject.file('buildSrc/src/main/groovy/shared-common.gradle')
}
